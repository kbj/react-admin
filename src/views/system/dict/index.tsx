import type { FC, ReactNode } from 'react'
import React, { memo } from 'react'

interface IProps {
  children?: ReactNode
}

/**
 * 字典管理
 */
const Dict: FC<IProps> = () => {
  // const { searchForm, queryParam, query, pageChange, data, loading } = usePage()
  return <></>
}

export default memo(Dict)
